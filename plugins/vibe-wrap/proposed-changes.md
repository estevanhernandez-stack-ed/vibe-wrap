# Proposed changes — vibe-wrap

> Generated by `/vibe-wrap:evolve-wrap` on 2026-06-09.
> Window: last 30 days (2026-05-10 → 2026-06-09). Sessions read: 1. Friction entries: 0 (`friction.jsonl` does not exist). Wrap docs scanned: 1.
> **This is a proposal. Nothing here is applied automatically. Review each item and apply individually.**

## Observed patterns

One paired session in the window — the 2026-05-23 wrap on `626labs-hub` (sentinel + terminal, UUID `ea808012`, outcome `completed`, plugin_version 0.2.0). One wrap doc (`626labs-hub/docs/session-wraps/2026-05-23-2349.md`). Zero friction entries. Zero breadcrumbs. That's thin for behavioral inference, so this cycle holds every frequency-based pattern and proposes only what the data proves deterministically: contract violations and defects with a verified root cause. Confidence grades below follow that split — frequency-based grading for behavior, root-cause certainty for defects.

1. **Mojibake in git-sourced wrap-doc lines.** The 2026-05-23 doc renders `â€”` where commit subjects contain an em-dash (the vibe-insights `R4 polish` line, the vibe-plugins `remove drafts/vibe-wrap` line). Root cause verified: `skills/wrap/scripts/git-state.py` `run_git()` (lines 102-108) calls `subprocess.run(..., text=True)` with no `encoding=` — on Windows that decodes git's UTF-8 output as cp1252. Every other subprocess call site in the plugin passes `encoding="utf-8"` (`render-wrap.py:153`, `decision-log/file_jsonl.py:139`); `run_git` is the one miss, and it funnels every commit subject and status filename in the doc.

2. **The durable artifact contradicts its own session on gate outcomes.** The doc says `No decisions logged this session.` The same run's terminal session entry records `gates_accepted: ["decision_log"]` and `decision logged to dashboard (Ts5HjhCgBIr8azfXGs1C)`. The flow guarantees this whenever Gate 3 accepts: the doc renders at Step 2 (23:49), the decision logs at gate time (~23:59), and nothing writes back. The commit-gate redirect (cleanup PR #26) and the push + bridge declines are likewise invisible to the doc. The handoff artifact — the thing a future session reads — is permanently wrong about the session that produced it.

3. **The session log was hand-rolled, not script-written.** Sentinel and terminal share one timestamp (`23:59:27` — both appended at end; the sentinel never marked the start, which `start.py` provably does, and the doc itself predates both by 10 minutes). The terminal entry carries fields the schema doesn't define (`gates_accepted`, `notes`), omits documented ones (`mode`, `persona`, `user_pushback`, `friction_notes`, `key_decisions`, `artifact_generated`), and formats `complements_invoked` as `"626labs-cloud-mcp"` instead of the documented `"<source>:<name>"`. The session-logger SKILL's orchestrator-context fallback says fall back to process-notes — it never anticipated the third path that actually happened: hand-built JSONL that drifts. Implication for the long game: evolve-wrap's own input becomes unparseable the moment two runs drift differently.

4. **Friction wiring failed its first live audit.** The session provably hit two rows of the friction-trigger map — push gate declined (`default_overridden`, low) and bridge gate declined after the threshold fired (`complement_rejected`, high, `complement_involved: mcp__626labs-cloud__bridge_context_to_architect`) — both recorded in the terminal entry's `notes`. `friction.jsonl` was never created. The wrap SKILL's entire friction surface is one prose pointer (`skills/wrap/SKILL.md:154`); no call site sits at the gates where the triggers live. The map↔call-site bidirectional consistency this command is chartered to audit (guide SKILL line 68) fails on its first exercise. Until this wires, evolve-wrap runs blind on exactly the signal it was built to read.

5. **Breadcrumb adoption is zero; git carried the whole wrap.** `breadcrumbs/` has been empty since creation (5/10). No sibling plugin wrote a session log on 5/23 despite cart, taker, and keystone shipping commits that day. 100% of the doc's content came from multi-repo git state plus the decision-log backend — yet the banner still opens `Generated by vibe-wrap from the breadcrumb trail your toolkit left this session.` False in the only observed run. The multi-repo git read (v0.2.0's bet) is the part that actually paid: 10 repos, 54 commits, correctly attributed.

6. **Version note, no edit needed.** The installed cache is 0.2.0 while stable pins v0.2.1 — the 5/23 session ran the pre-MCP-name-fix build. The v0.2.1 genericize sweep is confirmed clean in the solo repo (no stale `mcp__626Labs__*` remnants). User-side `/plugin marketplace sync` + update is the move; nothing for the SKILLs.

7. **Stale template footnote.** `skills/evolve-wrap/references/proposed-changes-template.md` line 44 still lists multi-repo wraps as a default deferred item. Multi-repo shipped in v0.2.0 (`f034360`).

## Proposed SKILL edits

| File | Proposed change | Answers pattern | Confidence | Status |
|---|---|---|---|---|
| `skills/wrap/scripts/git-state.py` (lines 102-108) | Add `encoding="utf-8", errors="replace"` to `run_git()`'s `subprocess.run` call, matching the discipline every other call site already follows. | #1 | high (deterministic root cause; one-line fix) | applied 2026-06-09 |
| `skills/wrap/SKILL.md` (Step 4, lines 131-137) | Post-gate write-back: after the gates resolve and before `session-logger.end()`, append a `## Gate outcomes` block to the just-written wrap doc — decision-log result (backend + ref ID), commit/push outcomes, bridge outcome. Pure append to the doc this command just wrote; no render-script change required. Skip when `--inline-only`. | #2 | medium (n=1, but flow ordering guarantees recurrence whenever Gate 3 accepts) | applied 2026-06-09 |
| `skills/wrap/SKILL.md` (Step 3, lines 91-129 + line 154) | Inline per-gate friction wiring: at each gate's resolution step, name the exact friction-triggers row and the `friction-logger/scripts/log.py` invocation to fire (push declined → `default_overridden`/low; bridge declined when threshold fired → `complement_rejected`/high with `complement_involved`; commit-message rewrite → `artifact_rewritten`/medium). Replace the single prose pointer with call sites where the triggers live. | #4 | medium (single session, but the contract audit already failed once and the fix is additive) | applied 2026-06-09 |
| `skills/session-logger/SKILL.md` (§ Orchestrator-context fallback, lines 162-168) | Close the hand-rolled-JSONL path: in orchestrator context the SKILLs MUST still shell out to `start.py` / `end.py` (plain stdlib Python, no runtime coupling — this very evolve run did it). Process-notes fallback only when shelling out is genuinely impossible. Hand-built session entries are forbidden; if one must be written, it conforms to the documented schema exactly. | #3 | medium (one drifted run observed; deterministic-class violation) | applied 2026-06-09 |
| `skills/wrap/assets/wrap-template.md` (line 3) + `skills/wrap/scripts/render-wrap.py` (template assembly) | Honest banner: when the run found zero breadcrumbs, the banner reads `Reconstructed from git state + sibling logs (no breadcrumbs found this session).` Keep the current line only when breadcrumbs actually contributed. | #5 | medium (n=1 doc, but the condition is deterministic at render time) | pending |
| `skills/evolve-wrap/references/proposed-changes-template.md` (line 44) | Drop "multi-repo wraps" from the default-deferred footnote — shipped in v0.2.0. Keep the `PreToolUse` hook and `:digest` entries. | #7 | high (factual staleness; zero ripple) | pending |

## Proposed config changes

None this cycle. The one observed session used `backend: 626labs-mcp` with `auto_detect_mcp: true` and the append succeeded (dashboard ref `Ts5HjhCgBIr8azfXGs1C`) — the config schema held. One declined bridge prompt is noise, not threshold evidence; tuning the bridge threshold before friction wiring (edit 3) lands would be moving a dial with no gauge attached.

## Deferred — what didn't make the cut

- **Bring-your-own decision-log MCP** (hand-seeded 2026-05-23: let the `mcp` backend target an arbitrary server's tool names, e.g. `mcp__<their-server>__<their-log-tool>`, not only the auto-detected 626Labs dashboard `mcp__626labs-cloud__*` — the v0.2.1 reframe made the language generic + file-fallback universal; this makes the MCP backend itself bring-your-own) — **status: picked up by sprint task T4a** (cross-plugin work, current sprint). Not re-proposed here; tracking moves to the sprint. Original provenance: 2026-05-23 evolve-input sweep.
- **Breadcrumb sibling adoption** (getting cart / doc / test / taker to actually call `:plant`) — held because it's cross-plugin coordination outside vibe-wrap's no-sibling-writes contract. The zero-breadcrumb finding (#5) makes the case, but the fix lands in sibling repos and the marketplace, not in this plugin's SKILLs. Needs a deliberate cross-plugin decision, same class as T4a.
- **`PreToolUse` breadcrumb-autodetect hook** — template default, and with zero breadcrumbs it's the tempting fix-shaped thing; held because one session of absence doesn't justify the noise-filtering work. Revisit if adoption (above) stalls another cycle.
- **Cross-session digests (`:digest`)** — template default; no new signal.
- **Behavioral threshold tuning** (bridge threshold, commit-gate prompt copy) — held at n=1 session. Needs 3+ wraps with friction wiring live before any dial moves.
- **`:reconnect`-style session-log backfill** for orchestrator-run sessions — forward note already in the session-logger SKILL; held until the hand-rolled-entry ban (edit 4) proves insufficient on its own.

## How to apply

1. Read each proposed edit against the pattern it answers. If the pattern is real and the edit is safe, apply it.
2. Apply edits one at a time with a `[y/n]` per item — never bulk-apply.
3. Re-run the affected SKILL or script against a real session to confirm the change holds (the git-state encoding fix is verifiable with one `git log` containing an em-dash).
4. Log any applied edit as a decision (the wrap flow's decision-log gate, or directly) so the next `evolve-wrap` cycle sees what changed and why.

---

*Naming note: this plugin uses `evolve-wrap`, not `evolve` — the first marketplace plugin under the `evolve-<short>` convention. See `drafts/_pending-renames.md` for the sibling-rename plan.*
